[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[tool.hatch.build]
sources = ["src"]

[project]
name = "jsnow17"
version = "0.2.0"
description = "Snow-17 temperature-index snow model -- symfluence plugin"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
authors = [{ name = "SYMFLUENCE Team", email = "dev@symfluence.org" }]

dependencies = [
    "symfluence>=0.7.0",
    "numpy",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
jax = ["jax", "jaxlib"]
dev = ["pytest>=7.0"]

[project.entry-points."symfluence.plugins"]
snow17 = "jsnow17:register"
